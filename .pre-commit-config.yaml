repos:
  - repo: local
    hooks:
      - id: trufflehog
        name: TruffleHog
        description: Detect secrets in your data.
        entry: echo "Local hook is running"
        language: system
        stages: ["commit", "push"]
